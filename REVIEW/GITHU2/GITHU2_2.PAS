var
    j,q,so2,so5,dem,res,x : Longint;
    n,k,i : Int64;
    f,g : Text;
begin
    assign(f,'GITHU2.INP');
    reset(f);
    assign(g,'GITHU2.OUT');
    rewrite(g);
    read(f,x);

    for i := 1 to n do
    begin

    end;



    close(f);
    close(g);
end.
