type
    a : List = array[1..100000] of Longint;
var
    i,n,t : Longint;
    f : Text;
begin
    assign(f,'GRAPH.INP');
    reset(f);
    readln(f,n,m);
    for i := 1 to m do
    begin

    end;
    close(f);

    assign(f,'GRAPH.OUT');
    rewrite(f);
    close(f);
end.